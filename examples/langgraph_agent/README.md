# LangGraph Fused Chat Agent

This experimental example showcases a minimal LangGraph `StateGraph` that
streams responses while recording traces to Langfuse via `coaiapy` helpers.
Use it as a starting point for more advanced workflows.

Run it with Python:
```bash
python main.py
```
